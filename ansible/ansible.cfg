[defaults]
inventory = /myhosts
host_key_checking = False
roles_path = /etc/myroles
private_key_file = /root/myplaybooks/aws-private.pem
remote_user = ec2-user
[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false

